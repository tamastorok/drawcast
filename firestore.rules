rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read access to all documents
    match /{document=**} {
      allow read: if true;
    }
    
    // Users collection rules
    match /users/{fid} {
      allow write: if request.resource.data.fid == fid;
    }
    
    // Games collection rules
    match /games/{gameId} {
      allow create: if request.resource.data.userFid == request.resource.data.fid;
      allow update: if request.resource.data.userFid == request.resource.data.fid && 
        (request.resource.data.diff(resource.data).affectedKeys().hasOnly(['guesses', 'totalGuesses', 'correctGuesses']));
    }
    
    // Prompts collection rules
    match /prompts/{promptId} {
      allow write: if false; // Only admin can write prompts
    }
  }
}